# 实践分支：场景 2-A（产品设计 · 新模块/新功能）

| 字段 | 内容 |
|------|------|
| 分支编号 | 2-A |
| 业务线 / 探索载体项目 | 广发证券中后台系统（证金业务平台、员工投资行为系统） |

---

## （1）需要用到哪些知识？

| 知识类别 | 具体内容 | 示例路径/文档名 |
|----------|----------|-----------------|
| 设计系统基础理论 | 设计 Token（颜色、间距、字体、阴影、圆角）、8px 基准网格、分层架构（Foundation/Components/Pages） | `design-specs/` |
| Figma 设计稿解析 | 从 Figma 视觉稿中提取色值、字号、间距、组件状态、图层命名规则 | Figma 源文件：`CzM19wZsQTBqOj3lkwFqfh` |
| 图标设计规范 | SVG 导出规范、线型/填充风格、尺寸分级（12/16/20/24/32px）、Lucide 替代原则 | `design-specs/foundation/icons.md` |
| 页面模板规范 | 列表页/表单页/详情页/仪表盘页的标准结构、布局层次、业务组件组合方式 | `design-specs/pages/*.md` |
| AI 提示词工程 | 结构化需求描述、设计规范引用、约束指令（如禁止一次性读取全部 Markdown）、角色定位 | `gf-design-system.md` |
| 金融中后台业务知识 | 线上典型页面 | 测试/生产环境 |

---

## （2）知识在哪里获取？获取困难与对策

| 知识项 | 获取位置 | 是否存在获取困难 | 已解决做法 / 建议 |
|--------|----------|------------------|-------------------|
| Figma 设计稿视觉规范 | Figma 文件 `中后台系统组件库` | **否**。Figma 为设计工具，Token 分散在各图层属性中，无统一导出表 | 通过mcp工具建立 `design.md` 作为统一规范源；按 Foundation/Components/Pages 三层重新组织 |
| 业务组件交互细节 | 业务组件.pdf | **是**。PDF 为静态文档，各类规则需人工2次确认 | 提取关键规则写入 `design-specs/components/business.md`，附使用示例 |
| 图标资源 | Figma 图标页面 + GF Icon 官方库 | **否**。通过mcp工具批量导出 | 统一导出为 SVG，按 `common/`、`status/` 分类存放；建立 `icons.md` 索引表 |
| 金融业务字段含义 | 业务方需求文档/口头沟通 | **是**。业务术语（如"券一下"、"锁券余额"）非通用 | 在提示词和典型页面与 prd 中直接保留业务原始术语，通过页面上下文让 AI 推断含义 |

---

## （3）知识使用困难与对策

| 困难描述 | 已解决做法 / 建议 |
|----------|-------------------|
| AI 上下文窗口有限，单文件过大导致无法完整读取设计规范 | **拆分策略**：将 1700+ 行的 `design.md` 拆分为 18 个独立子文档（foundation 7 个 + components 6 个 + pages 4 个 + README 索引），每个文档聚焦单一主题 |
| AI 难以判断"用户查询应该读哪个子文档" | **索引映射表**：在 `README.md` 中建立"关键词 → 目标文件 → 备选文件"的调用映射表，并强制约定"单一文件原则" |
| AI 生成界面时容易遗漏细节（如阴影方向、圆角大小、禁用态透明度） | **Token 化 + 示例代码**：将设计规则转为 CSS 变量和代码片段，AI 可直接复制使用；在 `tokens.md` 中汇总全部变量 |
| AI 使用图标时风格不一致（线型/填充混用）或找不到对应 SVG | **图标索引 + 替代原则**：`icons.md` 列出全部 58 个 SVG 路径；约定找不到时去 Lucide 搜索同类型线性图标，线条粗细 1.5px~2px |
| AI 生成页面时组件组合随意，不符合金融中后台习惯 | **页面模板层**：在 `pages/` 下预定义 4 种典型页面（列表/表单/详情/仪表盘），明确每层结构、引用的组件文档、布局参数 |
| 提示词中业务需求描述过长，AI 容易遗漏控件 | **结构化分层描述**：按"顶部/导航/主内容区/模块/控件"逐层拆解，每个控件标注规格（尺寸、颜色、数据格式） |

---

## （4）知识梳理/治理做法与困难

| 治理做法 | 困难 | 已解决 / 建议 |
|----------|------|---------------|
| **Figma → Markdown 人工转录**：从 Figma 视觉稿整理并提取（如无组件库）颜色、字体、间距、组件状态，转为结构化 Markdown 表格和代码块 | 工作量大（约 8 大分区、50+ 组件、数百个状态），且 Figma 更新后需重新比对 | 建立"Figma 页面 → Markdown 文件"的一一映射关系（见 `design-specs/README.md` 第 3 节），降低后续维护成本 |
| **设计 Token 统一命名**：颜色按"品牌主色/红/橙/中性色/功能色"分级，间距按 `space-0~16` 编号，避免分散命名 | 初期 Figma 中 Token 命名不统一（如 `点文本-特粗/96pt` 与 `中性色/中性/N3（分隔线）` 混用） | 在 `tokens.md` 中建立统一的 CSS 变量名（如 `--color-primary`、`--space-4`），与 Figma Token 做对照表 |
| **组件状态全枚举**：对每个交互组件（按钮、输入框、菜单等）列出 normal/hover/active/disabled/loading 等全部状态的色值和样式 | 状态组合爆炸（如按钮 7 类型 × 3 尺寸 × 5 状态），文档膨胀 | 采用"类型定义 + 状态表"的模式，共用同一套状态描述，避免重复 |
| **图标 SVG 手工导出与优化**：从 Figma 导出 48 个通用图标 + 13 个状态图标，统一设置 `overflow: visible`、线条宽度 1.5px | SVG 路径在不同尺寸下可能失真；Figma 导出时部分图标未合并路径 | 导出后在代码中统一使用 `shape-rendering: geometricPrecision`，并约定优先使用内联 SVG |
| **页面 Demo 验证**：将规范应用于实际业务页面（证金业务平台仪表盘、私募基金详情页），检验规范可行性 | 规范与实际业务需求存在 gap（如渐变卡片、组合图表在基础规范中未定义） | 在业务 Demo 中补充"渐变样式"、"图表组合卡片"等特殊规则，并反向沉淀到 `components/business.md` |

---

## （5）知识组织做法与困难

| 组织方式（目录/元数据/分层） | 困难 | 已解决 / 建议 |
|-----------------------------|------|---------------|
| **三级分层目录**：`foundation/`（底层 Token）→ `components/`（中层组件）→ `pages/`（顶层页面模板），外加独立 `icons/` 资源层 | 层级边界有时模糊（如"筛选区"是组件还是页面的一部分？"图表"是基础组件还是业务组件？） | 明确划分原则：foundation 无业务语义；components 可跨页面复用；pages 是完整业务场景。筛选区/图表归入 `components/business.md`，因为跨页面复用 |
| **索引文档（README.md）作为检索入口**：集中管理调用映射表、文件摘要、维护规则 | 索引文档本身也会膨胀，若规则描述过多会消耗 Token | 将约束规则精简为 4 条（单一文件原则、页面优先于组件、Token 溯源优先、图标直达），并附 1 张映射表 + 1 张摘要表 |
| **文件内容摘要**：为每个子文档提供 1 句话摘要，辅助 AI 快速判断调用目标 | 摘要需要随文件内容更新而更新，容易遗漏 | 在 `README.md` 第 5 节集中维护所有文件摘要，每次修改子文档后同步更新 |
| **层级依赖关系图**：用 ASCII 图展示 foundation → components → pages 的引用关系 | 纯文本图难以表达复杂依赖（如 pages 同时引用多个 components） | 简化依赖描述，仅标注"某 page 主要引用哪些 components"，不追求完整依赖图 |
| **版本与日期元数据**：每份文档末尾标注版本号、更新日期、更新人 | 目前仅 `design.md` 和 `tokens.md` 有更新日志，其余文档缺失 | 建议统一在 `README.md` 中维护全局变更日志，避免分散 |

---

## （6）知识检索做法与困难

| 检索方式 | 困难 | 已解决 / 建议 |
|----------|------|---------------|
| **关键词精确匹配**：在 `README.md` 中建立"用户查询关键词 → 必须调用的文件 → 备选文件"映射 | 用户查询词多样（如"主按钮颜色"vs"主要按钮 hover 色"），无法穷举 | 提取核心语义词（颜色/按钮/表格/列表页等），采用最左匹配策略；在 `tokens.md` 中汇总全部 Token，作为兜底检索目标 |
| **文件摘要快速判断**：AI 先读 README 摘要，再决定调用哪个子文档 | 摘要信息密度有限，某些细节（如"分段控制器 Segmented"）不易从摘要推断 | 在摘要中同时列出文件内的组件/主题清单（如 `business.md` 摘要包含"图表、筛选区、工具栏、底部操作栏"） |
| **图标路径直达**：用户明确询问某个图标时，直接引用 `icons/common/xxx.svg`，不经过 `icons.md` | 图标文件名与设计稿中的中文名称需人工对应（如 `arrow-down.svg` 对应"向下"） | 在 `icons.md` 中同时列出"文件名、图标名称、用途"三列，建立双语索引 |
| **页面模板优先**：若用户询问"某页面怎么设计"，直接调用 `pages/` 下对应文件，不分散查询组件 | 页面文档篇幅较长（如 `list-page.md` 含 12 层结构），可能超出单次读取限制 | 页面文档本身也采用分层标题结构，AI 可定位到具体章节；必要时将页面文档进一步拆分为"结构层"和"细节层" |

---

## （7）知识持续更新

| 触发条件 | 责任人 | 更新方式与频率 |
|----------|--------|----------------|
| Figma 设计稿发生视觉变更（如品牌色调整、新增组件变体） | 交互与视觉设计组 | 同步修改 `foundation/` 下对应文档（优先 `tokens.md`），并检查 `colors.md` / `typography.md`；更新 `README.md` 摘要 |
| 新增业务组件（如新增一种图表类型或筛选控件） | 前端开发/设计组 | 在 `components/` 下选择最接近的分类追加；若新增分类则新建文件并更新 `README.md` 第 3、5 节 |
| 新增典型页面模板（如新增审批流程页） | 前端开发/设计组 | 在 `pages/` 下新建文件，基于现有组件组合推断补全，并更新 `README.md` 第 3、5 节 |
| 新增或替换图标 | 视觉设计组 | SVG 放入 `icons/common/` 或 `icons/status/`，在 `icons.md` 添加路径记录，无需修改 `README.md` |
| AI 使用规范时发现规范缺失或矛盾 | AI 使用者（开发者/产品经理） | 在对应子文档中追加缺失规则，或在 `pages/设计规范修正.md` 中记录临时补丁，定期合并到主文档 |
| 季度例行审查 | 设计规范维护人 | 每季度检查一遍 `README.md` 索引与实际文件是否一致，清理失效链接，更新版本号 |

---

## （8）使用的工具

| 工具 | 如何获取 | 在知识工程中的作用 |
|------|----------|-------------------|
| Figma | 官方订阅 / 企业账号 | 设计规范的唯一视觉源头；用于提取颜色、字体、间距、组件状态、图标路径 |
| Trae IDE | 字节跳动出品，官网下载 | AI 辅助编程环境；通过 Skill 机制挂载设计规范知识库，实现"规范即提示" |
| Web 浏览器 + DevTools | 系统自带 | 预览 AI 生成的 HTML Demo，检查样式是否符合规范；调试阴影、间距、颜色偏差 |
| Lucide Icons | `https://lucide.dev/icons/` | 当 GF Icon 库缺少所需图标时，按规范替代原则搜索风格一致的线性图标 |
| Node.js / Vite | 官网下载 / pnpm 安装 | 初始化前端项目（React/Vue），验证规范在真实代码中的可行性 |
| Markdown 编辑器 | VS Code / Trae 内置 | 编写和维护全部规范文档；利用目录大纲快速跳转 |

---

## （9）使用的 Skill

| Skill 名称 | 内容摘要或全文/链接 | 适用环节 |
|------------|---------------------|----------|
| `gf-design-system`（广发中后台系统设计规范） | 基于 Figma 设计稿提取的完整设计系统，包含 7 个基础规范文档、6 个组件文档、4 个页面模板文档、58 个 SVG 图标及调用索引。核心约束：8px 网格、品牌蓝 `#2A6CDD`、单一文件读取原则、Token 溯源优先。 | AI 生成任何中后台界面时，作为首选设计规范 Skill 挂载；适用于需求分析、页面设计、组件选型、样式校验 |
| `web-dev`（Web 应用开发指南） | 生产级 Web 界面开发 Skill，包含完整工作流：PRD → 技术架构 → 开发。内置文档生成模板（`web-docs-guideline.md`）、开发规范（`web-dev-guideline.md`）、质量评估标准。支持 React/Vue + Express 全栈项目脚手架初始化。 | 从零创建 Web 项目时的主流程 Skill；负责 PRD/技术文档生成、项目初始化、代码开发全流程管控 |
| `web-dev-guideline`（Web 开发技术规范） | 嵌套在 `web-dev` Skill 内的技术执行层，包含：项目结构规范、代码质量标准（组件 <200 行）、测试指南、React/Vue 编码规范、状态管理（zustand）、路由规范、图标使用规范等。 | 开发阶段的具体编码约束；确保代码可维护性、组件化、类型安全 |

### Skill 协作模式

本次实践中 `gf-design-system` 与 `web-dev` 的协作方式：

```
用户需求
    │
    ├── web-dev Skill ────→ 生成 PRD + 技术架构文档
    │                        （.trae/documents/）
    │
    ├── gf-design-system →  提供设计 Token、组件样式、图标资源
    │                        （design-specs/）
    │
    └── web-dev-guideline → 代码质量约束（组件拆分、命名规范等）
                             （skill/web-dev-guideline.md）

    ↓ 三者融合输出
ruixiang-rongtong-demo.html（纯 HTML Demo，含完整业务流程）
```

**协作中的冲突与解决**：

| 冲突点 | gf-design-system 要求 | web-dev-guideline 要求 | 解决方案 |
|--------|----------------------|-----------------------|----------|
| 图标来源 | 使用 `icons/` 目录的 SVG（fill 路径） | 使用 `lucide-react` 组件库 | **gf-design-system 优先**：中后台场景下 icon 库风格统一性 > 第三方库便利性 |
| 项目形式 | 验证用 HTML Demo 即可 | 必须初始化 Vite + React/Vue 脚手架 | **按需选择**：本次为快速验证 Demo 用单文件 HTML；正式项目走完整脚手架 |
| 设计美学 | 金融中后台严谨风格（8px 网格、克制配色） | 追求"bold aesthetic direction"、避免 generic | **gf-design-system 优先**：金融场景下合规与一致性 > 创意表达 |
| 文档语言 | 中文（业务术语） | 与 user_input 语言一致 | **一致**：两者均要求跟随用户语言 |

---

## （10）其他有必要总结的内容

### 10.1 从"单一大文档"到"分层知识库"的演进

- **第一阶段**：`design.md`（约 1780 行）。包含全部规范，但 AI 单次读取无法消化，经常遗漏细节。
- **第二阶段**：`design-spec.md`（约 1110 行）。精简版，但仍为单文件。
- **第三阶段**：`design-specs/` 目录（18 个文件 + 58 个图标）。按 Foundation/Components/Pages/Icons 四层拆分，配合 `README.md` 索引，实现精准单文件调用。

### 10.2 规范落地的验证方式

- **静态验证**：AI 根据规范生成 HTML Demo ，人工检查颜色、间距、组件状态是否符合 Figma 中的设计规范、组件库。
- **动态验证**：将规范应用于真实前端项目（React/Vue），检查 Token 在代码中的可执行性（如 CSS 变量是否覆盖全场景）。

### 10.3 与业务需求的耦合程度

- 基础层（foundation）完全与业务无关，可复用到其他金融系统。
- 组件层（components）90% 通用，仅"业务组件"（图表、筛选区、工具栏）带金融中后台语义。
- 页面层（pages）的页面结构（列表/表单/详情/仪表盘）可跨业务复用。

### 10.4 当前知识库规模

| 层级 | 文件数 | 图标数 | 覆盖组件/页面数 |
|------|--------|--------|-----------------|
| foundation | 7 个 md | — | 颜色/字体/间距/阴影/圆角/图标/tokens |
| components | 6 个 md | — | 60+ 组件（含状态变体） |
| pages | 4 个 md | — | 4 种典型页面模板 |
| icons | — | 58 个 SVG | 通用 45 + 状态 13 |
| 合计 | 18 个 md + 1 README | 58 SVG | 约 70+ 设计条目 |

### 10.5 本次实践（睿享融通服务管理系统 Demo）的关键发现

**问题域**：基于"睿享融通服务设计方案.doc"生成完整业务流程 Demo，涉及多标签导航、侧边栏、筛选区、表格、表单弹窗、审批流等全量组件。

**发现的规范缺口与修正**：

| 缺口类型 | 具体问题 | 修正动作 |
|----------|----------|----------|
| **SVG 裁剪** | 手写 stroke circle 图标（info/warning）在浏览器中显示不全，6 轮尝试（r 收缩 + overflow:visible）均无效 | 确立"图标库优先"原则：所有图标必须来自 `icons/` 目录的 fill 路径 SVG；禁止手写圆形 SVG |
| **CSS 变量缺失** | `--radius-sm` 未定义导致侧边栏圆角为 0px（直角），排查困难 | 建立 Token 完整性检查机制：生成代码后扫描 `var(--\w+)` 确保全部有定义 |
| **组件语义不清** | `gf-sidebar__section` 的用途（一级分组 vs 二级分类）未在规范中明确界定 | 规范中每个组件必须包含**正向定义**（用于什么）和**反向约束**（不用于什么） |
| **属性值精度不足** | 规范中写"小圆角"而非"2px"，导致与参考文件不一致 | 规范中必须标注**具体数值**，不能仅用描述性语言 |
| **日期选择器图标** | 浏览器原生日历图标不符合设计风格 | 扩展 icon 库使用范围：不仅是页面图标，控件内嵌图标（日期选择器、Alert 等）也必须使用 icon 库 SVG |
| **布局对齐方案不稳定** | MultiTab 底部对齐经 6+ 轮迭代才稳定 | 布局类问题应在方案阶段确定**完整参数组合**，避免单参数逐次调试 |

---

## 成功经验

| 知识侧做法 | 对 AI 辅助质量的提升 | 可复制要点 |
|------------|----------------------|------------|
| **分层拆分 + 索引映射**：将大文档拆分为 18 个子文档，通过 README 关键词映射实现精准调用 | AI 不再"囫囵吞枣"，每次只读最相关的 1 个文件，输出准确率大幅提升；Token 消耗降低约 60% | 任何领域知识库都可采用"3~4 层分层 + 中央索引"模式；索引中必须包含"关键词→文件"的显式映射 |
| **设计 Token 化（CSS 变量）**：颜色、间距、阴影、字体全部转为 `--token-name` 形式 | AI 生成代码时直接引用变量，避免硬编码色值；当设计变更时只需改 Token，不用改每处代码 | Token 命名采用"属性+语义"双维度（如 `--color-primary`、`--space-4`），既易读又易检索 |
| **页面模板预定义**：提前定义列表/表单/详情/仪表盘 4 种页面的完整结构层次 | AI 接到业务需求时，先匹配页面模板，再填充组件，避免"从零乱搭"；输出结构符合金融中后台用户习惯 | 页面模板应包含"第 N 层结构 + 该层引用的组件文档 + 布局参数"，形成可执行的组装说明书 |
| **结构化需求提示词模板**：按"顶部→导航→主内容→模块→控件"五层拆解需求，每个控件绑定规格 | AI 不会遗漏控件（如"时间选择器"、"分段控制器"）；生成的界面与需求一一对应 | 需求描述采用"层级缩进 + 粗体标注关键规格"的格式，模拟设计稿标注风格 |
| **图标库独立资源层**：58 个 SVG 按 common/status 分类，附索引和替代原则 | AI 能准确找到图标路径，且风格一致；不会出现"用 emoji 代替图标"或"风格混用"的问题 | 图标资源与文档分离，文档中只存索引；替代原则明确到线条粗细和尺寸 |
| **参考文件对齐法**：当存在可参考的已实现页面时（如同系统的另一模块），逐项对比 CSS 属性值（颜色、圆角、间距、字号、过渡动画），而非凭记忆或猜测 | 避免了多轮反复调整（如侧边导航的 hover 背景色、菜单项圆角、箭头尺寸、子菜单容器背景等细节差异） | 建立"参考文件 → 差异对比表 → 统一修正"的工作流；规范中应标注每个属性的具体数值而非仅描述意图 |
| **图标库优先原则**：需要图标时先查 `icons.md` 索引表，使用 `icons/common/` 或 `icons/status/` 中已有的 SVG（fill 路径，无裁剪风险）；禁止手写 stroke circle 等易出问题的 SVG | 彻底解决了 SVG 圆形图标在浏览器中被裁剪显示不全的问题（尝试 r=10→9→8→7 + overflow:visible 均无效后，换用 icon 库 fill 图标一次解决） | 规范中应强制约定："所有图标必须来自 icon 库，禁止手写 SVG"；日期选择器、Alert 等组件的图标也应使用 icon 库 |
| **CSS 变量完整性检查**：在生成代码前确认所有引用的 CSS 变量已在 `:root` 中定义 | 避免了 `--radius-sm` 未定义导致圆角为 0px（直角）的问题；此类问题难以通过视觉排查发现 | 建立 Token 定义清单（tokens.md），每次新增变量名时同步更新；代码生成后做 `var(--xxx)` 全量扫描 |

---

## 不成功经验

| 已做做法 | 仍无效的主要原因 | 建议 |
|----------|------------------|------|
| 初期尝试用单一大文档（`design.md`）承载全部规范 | AI 上下文窗口有限，读取大文档后常遗漏后半部分；组件状态、页面细节被忽略 | 必须拆分。单文档超过 500 行就应考虑分层 |
| 在提示词中直接粘贴大量设计规范文本 | 提示词中混杂需求描述和规范文本，导致 AI 混淆"要做什么"和"怎么做" | 将规范封装为 Skill（知识库），与需求分离；AI 先读 Skill 建立上下文，再读需求执行任务 |
| **手写 stroke circle SVG 作为图标**（如 Alert 的 info/warning 图标） | `circle r="9"` + `stroke-width="2"` 在 24×24 viewBox 中外边缘仅距边界 2px，小尺寸渲染时被裁剪。即使逐步收缩到 r=7 并加 `overflow:visible`，部分浏览器仍无法完整显示 | **禁止手写圆形 SVG**。统一使用 icon 库中的 fill 路径图标（如 `warning-fill.svg`、`info-fill.svg`），这些图标在 16×16 viewBox 中使用 fill 路径，无裁剪风险 |
| **未对齐参考文件的细节**就自行推断样式值 | 侧边导航的 hover 背景（#F5F5F5 vs #E8E8E8）、圆角（4px vs 2px）、选中态背景（rgba vs transparent）、箭头尺寸（16px vs 12px）、子菜单容器背景等 10+ 处细节与参考不一致 | 必须有参考文件时，**逐属性对照**后再写代码；不要依赖记忆或"看起来差不多" |
| **误解导航层级语义**：将 `gf-sidebar__section` 用于一级分组标题 | 用户反馈后才发现 `gf-sidebar__section` 应仅用于子菜单内的二级内容分类（如"睿享融通""私享会"），一级菜单直接排列不需要分组标题 | 规范中对组件用途必须有明确的**正向定义**（用于什么场景）和**反向约束**（不用于什么场景），不能仅靠类名推测 |
| **MultiTab 标签底部对齐反复调参**（6+ 轮迭代）：依次尝试 top:1px / margin-bottom:-1px / padding 不对称 / border-bottom transparent / align-items 变化等 | 根因是同时操作多个相互影响的属性（padding、margin、border、align-items），每轮只改一个参数导致效果不稳定 | 对布局对齐问题应采用**单一方案一次性解决**：本例最终方案为 `align-items:flex-end + margin-bottom:-1px + border-bottom:1px solid transparent(非激活)/#FFF(激活)`，应在方案设计阶段就确定而非边试边改 |
| **CSS 变量名不一致**：代码中使用 `--radius-sm` 但 tokens.md 仅定义了 `--radius-small` | 变量解析为空值 = 0px = 直角，但视觉上不易察觉是"缺失"还是"故意为0" | 建立 Token 命名规范：同一概念只用一个名称（如统一用 `--radius-small` 或提供别名映射）；生成代码后做 `var(--\w+)` 正则扫描验证 |
| 未对 Figma 图层命名做规范化治理，直接按 Figma 原始命名转录 | Token 命名混乱（如 `点文本-特粗/96pt` 与 `中性色/中性/N3（分隔线）` 风格不一致），AI 难以建立语义关联 | 在转录阶段进行二次命名，建立统一的 CSS 变量命名体系，与 Figma 原始命名做对照表即可 |
| 图标导出后未统一优化，部分 SVG 路径存在冗余节点 | AI 在代码中内联 SVG 时，冗余路径导致文件膨胀；部分图标在 16px 下显示模糊 | 导出后统一规范：`overflow: visible`、线条 1.5px、`shape-rendering: geometricPrecision`；优先使用内联而非外部引用 |
| 页面 Demo 生成后仅做静态检查，未在真实框架（React/Vue）中运行 | 部分规范在纯 HTML 中可行，但在组件化框架中难以落地（如部分业务组件与开源组件库冲突） | 每类典型页面应在真实框架中至少实现一次，验证规范的可编码性 |